#include "main.h"

/**
 * _isdigit - This checks for a digit from 0 through 9
 * @c: int to be checked also
 * Return: 1 if c is a digit or 0 if otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
