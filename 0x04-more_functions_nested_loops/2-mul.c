#include "main.h"

/**
 * mul - This checks for a digit from 0 through 9
 * @a: a - variable
 * @b: b - variable
 * Return: Always 0
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
