#include "main.h"

/**
 * _puts - program that prints string to stdout.
 * @str: the string is to be printed.
 * _putchar prints a new line
 */
void _puts(char *str)
{
	while (*str)
		_putchar('\0');
}
