#include "main.h"
/**
 * _islower - program checks lowercase character
 * @c: character to check
 * Return: 1 if c is lowercase, and 0 if its not lowercase
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
