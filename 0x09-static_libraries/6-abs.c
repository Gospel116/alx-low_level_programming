#include "main.h"

/**
 * _abs - program that computes the absolute valur of an integer
 * @n: The int to be checked
 * Return: The absolute value of the int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
