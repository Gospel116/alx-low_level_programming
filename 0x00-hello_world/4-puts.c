#include <stdio.h>

/**
 * main - Entry point
 * Description: This program prints a string as the output
 * Return: o (success0
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
