#include <stdio.h>

/**
 * main - Entry point
 * Description: This programm prints a string as the output
 * Return: 0 (success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
